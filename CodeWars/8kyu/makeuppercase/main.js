function makeUpperCase(str) {
    // Code here
    return str.toUpperCase();
}

frase = "Alexandre";

console.log(makeUpperCase(frase));

// The toUpperCase() method converts a string to uppercase letters.
// string.toUpperCase()
